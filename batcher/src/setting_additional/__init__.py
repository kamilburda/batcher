"""Additional settings used in the plug-in.

These settings are tailored to specific uses and are dependent on many other
modules, hence their separation from the `setting` package.
"""

from .presenters_gtk import *
from .settings import *
